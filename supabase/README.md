# Supabase Usage

Supabase is used for authentication only.

Do not create or apply business database migrations in Supabase for this deployment. Business and application data are stored in the local PostgreSQL database managed by the Express API.

Old Supabase business migrations were moved to:

```text
docs/legacy-supabase-business-migrations/
```
